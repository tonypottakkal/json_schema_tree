{
    "$id": "https://gitlab.graalquest.org/phBlueMaritimeObjectSchema",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Schema version 2.5 for representing maritime vessels in a modular design to support their various properties and updates from selected Data Providers, including operational status and capabilities. Developed by Jeremy Mize for AF TENCAP TALON Powderhorn.",
    "type": "object",
    "properties": {
        "overallClassificationMarking": {"type": "string", "maxLength": 120, "description": "Security classification marking for the aircraft, including entities authorized to receive aircraft information."},
        "objectUUID": {"type": "string", "format": "uuid", "description": "Unique identifier for the maritime vessel."},
        "hullNumber": {"type": "string", "maxLength": 10, "description": "Hull number of the vessel."},
        "vesselName": {"type": "string", "maxLength": 20, "description": "Name of the vessel."},
        "vesselType": {"type": "string", "maxLength": 20, "description": "Type or class of the vessel."},
        "sconum": {"type": "string", "maxLength": 7, "description": "Service craft operator number."},
        "mmsi": {"type": "string", "maxLength": 10, "description": "Maritime Mobile Service Identity number."},
        "operator": {"type": "string", "maxLength": 5, "description": "Operator code of the vessel."},
        "affiliation": {"type": "string", "enum": ["CIV", "MIL", "GOV", "UNK"], "description": "Affiliation of the vessel (civilian, military, government, unknown)."},
        "nationality": {"type": "string", "pattern": "^[A-Z]{3}$", "description": "Three-letter country code associated with the vessel."},
        "equipmentCode": {"type": "string", "maxLength": 5, "pattern": "^[A-Za-z0-9]{1,5}$", "description": "Alphanumeric code representing equipment, maximum 5 characters, e.g., AB45Z."},
        "status": {
            "type": "object",
            "properties": {
                "latDD": {"type": "number", "minimum": -90, "maximum": 90, "description": "Latitude of the vessel."},
                "lonDD": {"type": "number", "minimum": -180, "maximum": 180, "description": "Longitude of the vessel."},
                "course": {"type": "number", "minimum": 0, "maximum": 360, "description": "Course of the vessel."},
                "heading": {"type": "number", "minimum": 0, "maximum": 360, "description": "Heading of the vessel."},
                "speed": {"type": "number", "minimum": 0, "description": "Speed of the vessel in knots."},
                "navStatus": {"type": "string", "maxLength": 20, "description": "Navigational status of the vessel."},
                "alt": {"type": "number", "description": "True altitude of the aircraft in feet above Mean Sea Level (MSL). Received as meters MSL, can also receive the 'altFt' variable, which converts the MSL to feet."},
                "identity": {"type": "string", "enum": ["Friend", "Foe", "Hostile", "Unknown"], "description": "Identification status of the vessel in operational terms."},
                "semiMajor": {"type": "number", "description": "The semi-major axis length, representing the longest radius of an ellipse in units (e.g., meters)."},
                "semiMinor": {"type": "number", "description": "The semi-minor axis length, representing the shortest radius of an ellipse in units (e.g., meters)."},
                "lastUpdated": {"type": "string", "format": "date-time", "description": "The timestamp of when the update was received from the sensor or data provider."},
                "sourceName": {"type": "string", "maxLength": 100, "description": "The name of the original source from which the data provider received the data."},
                "sourceObjId": {"type": "string", "maxLength": 100, "description": "The identifier for the data provider that supplied the data."},
                "dataMode": {"type": "string", "enum": ["REAL", "SIMULATED", "EXERCISE"], "description": "The mode of the data indicating whether it is real, simulated, or for an exercise."},
                "sourceClassification": {"type": "string", "maxLength": 60, "description": "The classification marking of the data as provided by the data provider."}
            },
            "required": ["alt", "identity", "latDD", "lonDD", "lastUpdated", "sourceName", "sourceObjId", "dataMode", "sourceClassification"],
            "additionalProperties": false
        },
        "ordnance": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "assetQty": {"type": ["integer", "null"], "description": "Quantity of the ordnance asset."},
                    "nalc": {"type": ["string", "null"], "maxLength": 10, "description": "Naval Ammunition Logistics Code (NALC) associated with the ordnance."},
                    "nomenclature": {"type": ["string", "null"], "maxLength": 100, "description": "The official naming of the ordnance."},
                    "lastUpdated": {"type": ["string", "null"], "format": "date-time", "description": "The timestamp of when the ordnance data was last updated."},
                    "sourceName": {"type": ["string", "null"], "maxLength": 100, "description": "The name of the original source from which the ordnance data was received."},
                    "sourceObjId": {"type": ["string", "null"], "maxLength": 100, "description": "The identifier for the data provider that supplied the ordnance data."},
                    "dataMode": {"type": ["string", "null"], "enum": ["REAL", "SIMULATED", "EXERCISE"], "description": "The mode of the ordnance data indicating whether it is real, simulated, or for an exercise."},
                    "sourceClassification": {"type": ["string", "null"], "maxLength": 60, "description": "The classification marking of the ordnance data as provided by the data provider."}
                },
                "additionalProperties": false
            },
            "maxItems": 15
        },
        "fuel": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "productType": {"type": "string", "description": "Type of fuel product available on the vessel."},
                    "fuelQty": {"type": "number", "minimum": 0, "description": "Quantity of fuel available on the vessel."},
                    "lastUpdated": {"type": ["string", "null"], "format": "date-time", "description": "The timestamp of when the fuel data was last updated."},
                    "sourceName": {"type": ["string", "null"], "maxLength": 100, "description": "The name of the original source from which the fuel data was received."},
                    "sourceObjId": {"type": ["string", "null"], "maxLength": 100, "description": "The identifier for the data provider that supplied the fuel data."},
                    "dataMode": {"type": ["string", "null"], "enum": ["REAL", "SIMULATED", "EXERCISE"], "description": "The mode of the fuel data indicating whether it is real, simulated, or for an exercise."},
                    "sourceClassification": {"type": ["string", "null"], "maxLength": 60, "description": "The classification marking of the fuel data as provided by the data provider."}
                },
                "additionalProperties": false
            },
            "description": "Information about fuel types and quantities available on the vessel."
        },
        "associations": {
            "type": "array",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                    "assocObjId": {"type": "string", "maxLength": 40, "description": "UUID of the associated object."}
                },
                "required": ["assocObjId"],
                "title": "associations",
                "description": "Associations with other entities or objects."
            },
            "maxItems": 15
        },
        "latestObjectUpdate": {
            "type": "object",
            "properties": {
                "sourceName": {"type": "string", "maxLength": 24, "description": "Name of the data source."},
                "sourceObjId": {"type": "string", "maxLength": 40, "description": "Unique identifier of the data source object."},
                "lastUpdate": {"type": "string", "format": "date-time", "description": "Timestamp of the last update from this data source."}
            },
            "required": ["lastUpdate", "sourceName", "sourceObjId"],
            "description": "Information about the latest update received for the object."
        }
    },
    "required": ["objectUUID", "overallClassificationMarking", "sconum", "status", "affiliation", "latestObjectUpdate"],
    "additionalProperties": false
}
